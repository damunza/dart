// collection

void main(){
  // list defined by List built in method
  List first = ['Jack', 'Jill'];
  print(first[0]); // indexes begin from zero
  print('\n');
  print(first.length);  // length of a list
  print('\n');
  for(var n in first){
    print(n);
  }
  print('\n');// each element in the list

  /* 
  -one data type add ; <TYPE> between the variable name and its assignation
  -constant values ; const added between the equal sign and the list itself
  -copying lists into lists; [...list being copied]
  */

  // sets {};


  /*
  -empty curly brackets ; identify a hashmap
  <> {}; define an empty set
  Set <> var = {} ; define an empty list
   */
}